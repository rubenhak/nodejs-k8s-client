var Promise = require('the-promise');
var should = require('should');

var ClientFetcher = require('./client');

describe('experiment', function() {

    it('experiment-1', function () {
        
        return ClientFetcher()
            .then(client => {
            })
            .then(result => {
            })

    });

});